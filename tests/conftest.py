import pytest

# Uncommenting this and removing setup.cfg, everthing works as expected
# def pytest_runtestloop(session):
#     session.add_marker(pytest.mark.filterwarnings('ignore::package.CustomWarning'))
